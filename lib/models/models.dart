/// не разбит по файлам, так как очень мало структур

enum HomePageStatus {
  menu, /// отобразить поля
  loading, /// показать индикатор загрузки
  loaded, /// отобразить рендерер
  failed, /// ошибка(
}